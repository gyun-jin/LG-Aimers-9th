# Aimers 9기 팀 공통 코드 작성 및 제출 전 체크리스트

이 문서는 팀원이 사용하는 모델, 피처, 실험 폴더, 현재 점수와 관계없이 공통으로 적용하는 체크리스트입니다. 코드 작성 중, 모델 확정 전, `submit.zip` 생성 후, DACON 업로드 직전에 순서대로 확인하며, 하나라도 필수 항목이 미확인 상태면 제출하지 않습니다.

## 0. 즉시 제출 중단 조건

다음 중 하나라도 확인되지 않으면 ZIP을 업로드하지 않습니다.

- [ ] 현재 투구 이후에 확정되는 정보가 피처에 포함돼 있지 않다.
- [ ] 현재 투구의 실제 위치·코스·판정·결과·실제 구종·Trackman 측정값을 사용하지 않는다.
- [ ] 2025 Trackman 또는 평가 시점 이후 Trackman을 사용하지 않는다.
- [ ] test의 다른 행을 이용한 통계·빈도·누적·rolling·target encoding·보정이 없다.
- [ ] 외부 데이터, 금지된 사전학습 가중치, 원격 API를 사용하지 않는다.
- [ ] 학습과 추론의 피처 생성 결과가 동일하다.
- [ ] 새 환경에서 실제 `script.py` 실행을 완료했다.
- [ ] 실제 245,789행 규모의 부하 검증을 통과했다.
- [ ] ZIP 이름·내부 구조와 `submission.csv` 형식이 정확하다.
- [ ] 설치·추론 시간, RAM, ZIP 크기 제한을 통과했다.

위 문장은 모두 부정형 위험을 확인하는 항목입니다. 각 문장이 사실일 때만 체크합니다.

## 1. 문제와 타깃

- [ ] 한 행이 현재 투구 하나라는 것을 전제로 모델을 작성했다.
- [ ] 출력은 `control_success=1`의 확률이다.
- [ ] 0 또는 1로 반올림하지 않고 0~1 실수 확률을 출력한다.
- [ ] 다음 세 경우가 제구 실패라는 정의를 이해하고 있다.
  - 스트라이크존 가운데 부근으로 들어간 공
  - 스트라이크존에서 크게 벗어난 공
  - 포수의 요구 방향과 반대로 들어간 공
- [ ] 위 실패 판정에 필요한 현재 투구 사후 정보는 타깃 의미와 평가에만 사용하고 입력에는 사용하지 않는다.

## 2. 사용할 수 있는 데이터

- [ ] 공식 `train.csv`를 지도학습 행과 정답의 기준으로 사용한다.
- [ ] 평가 환경의 공식 `test.csv`만 추론 입력으로 사용한다.
- [ ] 공식 `sample_submission.csv`를 출력 ID·컬럼·순서 기준으로 사용한다.
- [ ] Trackman을 사용할 경우 공식 `trackman_history.csv`의 2019~2024 기록만 사용한다.
- [ ] 선수 명단, 로스터, 외부 야구 기록, 검색 결과, 비공식 통계 등 외부 데이터를 사용하지 않는다.
- [ ] test 배포본 5행은 형식 확인용일 뿐 학습·보정·분포 추정에 사용하지 않는다.
- [ ] 실제 평가 test는 245,789행이며 정답은 평가 서버에만 있다는 전제로 작성했다.

### 외부 모델·API

- [ ] 허용 범위를 벗어난 사전학습 모델이나 다운로드 가중치를 사용하지 않는다.
- [ ] 모든 모델 가중치는 공식 학습 데이터로 직접 학습한다.
- [ ] 학습 알고리즘 라이브러리와 외부에서 가져온 사전학습 모델·가중치를 구분했다.
- [ ] OpenAI, Gemini, Claude, 번역·검색 API, 원격 추론 서버 등 외부 API를 호출하지 않는다.
- [ ] `requests`, `urllib`, `socket`, `curl`, `wget`, `boto`, `gdown`, 원격 URL 다운로드가 추론 코드에 없다.
- [ ] API key, token, 계정 정보, 원격 저장소 경로가 ZIP에 없다.

## 3. 시점과 데이터 누수

- [ ] 현재 투구 직전까지 알 수 있는 정보만 사용한다.
- [ ] 현재 투구 이후 점수, 주자 변화, 타석 결과, 경기 결과를 사용하지 않는다.
- [ ] 현재 투구의 `control_success` 또는 이를 직접·간접 복원하는 정보를 사용하지 않는다.
- [ ] 현재 투구의 위치·코스·판정·결과·실제 구종을 사용하지 않는다.
- [ ] 공식 `asof_*` 컬럼은 운영 측이 제공한 투구 직전 과거 정보이므로 사용할 수 있다.
- [ ] 직접 만든 누적 피처는 대상 투구 이전 기록만 포함한다.
- [ ] 선수·팀 인코딩, 결측 대치, 평활 prior, 빈도 mapping을 학습 fold에서만 fit한다.
- [ ] target encoding은 학습 행 자신의 타깃을 제외한 cross-fit 방식이다.
- [ ] validation 시즌과 미래 시즌의 정답·통계·mapping을 과거 학습 피처에 사용하지 않는다.
- [ ] outer validation을 early stopping이나 feature selection에 직접 재사용할 경우 점수가 낙관적이라는 점을 알고 분리했다.
- [ ] 확률 보정기는 validation 정답을 미리 보지 않은 과거 OOF 예측으로 학습한다.

### 권장 시간 검증

- [ ] 2019~2020 학습 → 2021 검증
- [ ] 2019~2021 학습 → 2022 검증
- [ ] 2019~2022 학습 → 2023 검증
- [ ] 2019~2023 학습 → 2024 검증
- [ ] 모든 후보를 동일한 fold·행·평가식으로 비교한다.
- [ ] Brier는 낮을수록 좋다는 기준으로 선택한다.
- [ ] 한 시즌만 좋아지고 최근 다른 시즌이 크게 악화되지 않았는지 확인한다.

## 4. 평가 test 행 독립성

평가 데이터의 현재 행은 test의 다른 행 없이도 같은 예측이 나와야 합니다.

- [ ] test 전체 또는 일부에 대한 `groupby`로 현재 행 피처를 만들지 않는다.
- [ ] test 내부 `value_counts`, 빈도, 평균, 중앙값, 분위수, 분포를 사용하지 않는다.
- [ ] test 선수별·팀별·월별 누적 통계를 사용하지 않는다.
- [ ] test 내부 rolling·expanding을 사용하지 않는다.
- [ ] test 내부 target encoding을 사용하지 않는다.
- [ ] test 행 순서 또는 앞뒤 행에 의존하지 않는다.
- [ ] test 전체 예측 평균을 보고 확률을 사후 이동·보정하지 않는다.
- [ ] 학습 데이터에서 저장한 정적 mapping·prior·schema만 조회한다.
- [ ] 배치 추론을 사용하더라도 각 행의 피처와 예측은 다른 test 행에 의존하지 않는다.

### 필수 동적 독립성 검사

같은 기준 행에 대해 다음 test 변형을 만들고 예측을 비교합니다.

- [ ] 기준 행 하나만 입력
- [ ] 원본 전체 test에 포함
- [ ] 다른 행 순서 섞기
- [ ] 다른 행 일부 삭제
- [ ] 다른 행 여러 번 복제
- [ ] 다른 행의 수치·범주를 극단값으로 변경
- [ ] 위 모든 경우 기준 행 예측의 최대 절대 차이가 `1e-12` 이하

## 5. Trackman 사용 시 추가 검사

Trackman을 사용하지 않으면 마지막 항목에 미사용 사유를 기록하고 이 절을 종료합니다.

- [ ] `trackman_history.csv`가 train/test와 1:1 정답 테이블이 아니라 과거 로그라는 것을 전제로 했다.
- [ ] 현재 투구의 Trackman 행을 직접 연결해 현재 측정값을 사용하지 않는다.
- [ ] 2025 Trackman을 로드·추정·복원하지 않는다.
- [ ] 검증 시즌 S에서는 S-1년 말까지의 Trackman만 사용한다.
- [ ] 2025 test에서는 2024년 말까지 사전 저장한 과거 요약만 사용한다.
- [ ] test 행을 Trackman에 새로 매칭해 선수 mapping을 만들지 않는다.
- [ ] test 내부 다른 행을 이용해 Trackman mapping 신뢰도를 계산하지 않는다.
- [ ] 선수 mapping은 공식 train과 공식 과거 Trackman만으로 만든다.
- [ ] 모호하거나 복수 후보가 있는 경기·선수 연결은 제외한다.
- [ ] availability 플래그도 Trackman mapping을 사용하므로 동일한 규칙 검토를 적용한다.
- [ ] 익명 ID나 경기 연결을 추론해 사용하는 경우 해당 연결 방법이 운영 측 규칙상 허용되는지 확인했다.
- [ ] 연결 허용 여부가 불명확하면 해당 연결에서 파생된 피처를 최종 제출에서 제외한다.
- [ ] ZIP에는 Trackman 원본 CSV를 넣지 않고 필요한 사전 요약·mapping만 `model/`에 저장한다.

Trackman 결정 기록:

```text
사용 여부:
사용한 cutoff:
mapping 기준:
운영 측 허용 근거:
누수 검사 결과:
연결 피처 미사용 대비 fold별 변화:
```

## 6. 공통 학습 재현성

- [ ] 난수성이 있는 모든 과정의 seed를 고정하고 기록한다.
- [ ] 학습 데이터 범위, 피처 목록, 전처리, 검증 방식, 주요 설정을 실험 기록에 남긴다.
- [ ] 모델·피처·전처리 선택은 사전에 정한 동일한 검증 데이터와 평가식으로 비교한다.
- [ ] 앙상블을 사용하는 경우 구성과 가중치는 동일한 OOF 행에서 검증한다.
- [ ] 최종 예측에 기여하지 않는 모델·전처리·패키지는 제출물에서 제거한다.
- [ ] 재표본화·가중 학습·확률 보정을 사용하는 경우 최종 확률 품질을 별도로 검증한다.
- [ ] 미사용 모델·임시 실험 파일·학습 캐시는 제출물에서 제외한다.
- [ ] 팀에서 확정한 학습 기간과 설정으로 최종 산출물을 다시 생성한다.
- [ ] 최종 실행에 필요한 모델 버전, 설정, seed, 피처 schema, mapping, prior, 보정기를 함께 저장한다.

### 자료 제공 코드 표시

- [ ] PDF·베이스라인에서 그대로 사용한 코드는 `[자료 제공 코드 시작/끝]` 주석으로 표시한다.
- [ ] 수정한 코드는 `[자료 제공 코드 기반 수정]`과 수정 이유를 표시한다.
- [ ] 새 구현은 `[추가 구현]` 또는 프로젝트의 일관된 표시로 구분한다.

## 7. 학습·추론 피처 동등성

- [ ] 학습과 추론이 같은 `build_features()` 또는 동등한 공유 구현을 사용한다.
- [ ] 입력 컬럼 이름과 순서를 저장 schema로 강제한다.
- [ ] 누락·추가 컬럼이 있으면 조용히 진행하지 않고 오류를 발생시킨다.
- [ ] 범주 mapping, 미관측 범주 기본값, 결측 대치가 학습과 추론에서 동일하다.
- [ ] 각 저장 모델 또는 파이프라인이 기대하는 feature 이름·순서·dtype이 추론 시 정확히 일치한다.
- [ ] 무작위 1,000행 이상에서 학습용·제출용 피처 dataframe equality를 검사한다.
- [ ] 저장 후 다시 로드한 모델 예측이 저장 전 예측과 일치한다.
- [ ] 분류 확률 출력에서 `control_success=1`에 해당하는 값인지 클래스 순서와 출력 의미를 확인한다.

## 8. `script.py` 검사

- [ ] 평가 서버에서 `script.py`가 단독 실행된다.
- [ ] 로컬 절대 경로를 사용하지 않는다.
- [ ] 입력은 공식 `./open/`과 안내 자료의 `./data/` 경로 차이를 안전하게 지원한다.
- [ ] 데이터 디렉터리를 수정하거나 파일을 쓰지 않는다.
- [ ] 모델은 ZIP 내부 상대 경로 `./model/`에서 로드한다.
- [ ] 추론 시 모델을 새로 학습하지 않는다.
- [ ] 인터넷이나 외부 서버가 없어도 실행된다.
- [ ] CPU thread를 최대 6개로 제한한다.
- [ ] GPU가 없더라도 CPU fallback으로 실행된다.
- [ ] `output/`이 없으면 생성한다.
- [ ] 결과를 정확히 `./output/submission.csv`로 저장한다.
- [ ] 오류를 숨기거나 placeholder 예측으로 대신하지 않는다.
- [ ] 단계별 모델 로드·피처 생성·예측·저장 시간을 로그로 남긴다.

## 9. `submission.csv` 검사

- [ ] 컬럼이 정확히 `row_id`, `control_success` 두 개다.
- [ ] 컬럼 순서도 `row_id`, `control_success`다.
- [ ] 행 수가 평가 `test.csv`와 같다.
- [ ] `row_id` 집합이 test와 정확히 같다.
- [ ] `row_id` 순서가 `sample_submission.csv`와 정확히 같다.
- [ ] 중복 `row_id`가 없다.
- [ ] 누락 예측과 추가 예측이 없다.
- [ ] `control_success`에 NaN·무한대가 없다.
- [ ] 모든 예측값이 0 이상 1 이하다.
- [ ] 확률을 0/1로 반올림하지 않는다.
- [ ] 인덱스를 CSV에 저장하지 않는다.

## 10. `requirements.txt` 검사

- [ ] `pip install -r requirements.txt`로 설치된다.
- [ ] 추론에 실제 필요한 패키지만 포함한다.
- [ ] 하이퍼파라미터 탐색·notebook·시각화 등 학습·실험 전용 패키지를 제외한다.
- [ ] 버전을 고정한다.
- [ ] Python 3.11과 Ubuntu 22.04 x86_64에서 설치 가능한 버전이다.
- [ ] 평가 서버 기본 패키지와 충돌하지 않는다.
- [ ] 로컬 캐시 없이 새 환경에서도 설치를 확인한다.
- [ ] 설치 시간이 10분 이하다.
- [ ] 패키지 설치 후 추가 모델·데이터 다운로드가 필요하지 않다.

## 11. `submit.zip` 구조

실제 업로드 파일명은 반드시 `submit.zip`입니다.

```text
submit.zip
├── script.py
├── requirements.txt
└── model/
    └── 모델·schema·mapping·보정 파일
```

- [ ] ZIP 파일명이 `submit.zip`이다.
- [ ] `script.py`가 ZIP 최상위에 있다.
- [ ] `requirements.txt`가 ZIP 최상위에 있다.
- [ ] 모델 파일은 최상위 `model/` 아래에 있다.
- [ ] `submit/` 같은 추가 최상위 폴더가 ZIP 내부에 없다.
- [ ] `.DS_Store`, `__MACOSX`, `__pycache__`, `.pytest_cache`가 없다.
- [ ] 원본 train/test/Trackman CSV가 없다.
- [ ] 학습 코드, OOF, 로그, 실험 registry, 임시 모델이 없다.
- [ ] ZIP 압축 크기가 10GB 이하다.
- [ ] 압축 해제 크기가 32GB 이하다.
- [ ] ZIP을 새 임시 디렉터리에 풀어 구조를 다시 확인했다.

최종 파일 기록:

```text
submit.zip 절대 경로:
```

## 12. 평가 서버 환경·자원

| 항목 | 공식 제한 |
|---|---|
| OS | Ubuntu 22.04.5 LTS |
| Python | 3.11.15 |
| CPU | 6 vCPU |
| RAM | 28GB |
| GPU | NVIDIA L4, VRAM 22.4GiB |
| CUDA | 12.8 |
| 인터넷 | 패키지 설치 외 비활성화 |
| 패키지 설치 | 최대 10분 |
| 추론 실행 | 최대 10분 |
| ZIP | 최대 10GB |
| 압축 해제 | 최대 32GB |

- [ ] 245,789행에서 전체 실행 시간이 10분보다 충분히 짧다.
- [ ] 모델 로드, 피처 생성, 각 모델 예측, 보정, 저장 시간을 따로 측정했다.
- [ ] 최대 RAM이 28GB보다 충분히 작다.
- [ ] GPU 사용 모델이면 VRAM이 22.4GiB보다 충분히 작다.
- [ ] CPU only 실행도 검증했다.
- [ ] 6개보다 많은 CPU thread를 사용하지 않는다.
- [ ] 한도에 간신히 맞추지 않고 운영 여유를 둔다.

권장 안전 목표:

```text
설치 6분 이하
추론 6분 이하
RAM 22GB 이하
ZIP 3GB 이하
```

## 13. 깨끗한 환경 최종 실행

- [ ] 빈 임시 디렉터리에 `submit.zip`을 풀었다.
- [ ] 새 Python 3.11 가상환경을 만들었다.
- [ ] 캐시 없이 requirements 설치를 확인했다.
- [ ] 공식 5행 샘플에서 실행했다.
- [ ] 245,789행 고유 ID 부하 데이터에서 실행했다.
- [ ] 인터넷을 차단한 상태에서 모델 로드·추론에 성공했다.
- [ ] 생성된 `output/submission.csv`를 다시 읽어 형식을 검사했다.
- [ ] 프로세스 종료 코드가 0이다.
- [ ] stdout·stderr에 숨겨진 경고나 실패가 없다.
- [ ] 최종 검증한 ZIP과 실제 업로드 ZIP의 SHA-256이 같다.

최종 기록:

```text
ZIP 절대 경로:
ZIP SHA-256:
ZIP 압축 크기:
압축 해제 크기:
requirements 설치 시간:
모델 로드 시간:
245,789행 추론 시간:
최대 RAM:
최대 VRAM:
행 독립성 최대 차이:
검증 일시:
```

## 14. 평가 점수 확인

공식 지표는 Brier Skill Score입니다.

```text
Brier = mean((p_i - y_i)^2)
Reference Brier = r × (1-r)
Score = max(0, 100000 × (1 - Brier / Reference Brier))
```

- `p_i`: 제구 성공 예측 확률
- `y_i`: 실제 정답 0 또는 1
- `r`: 전체 평가 데이터의 실제 평균 제구 성공률이며 비공개

- [ ] Accuracy가 아니라 Brier와 공식 BSS를 기준으로 모델을 비교했다.
- [ ] Brier가 낮을수록 좋다는 방향을 확인했다.
- [ ] 로컬 점수와 서버 점수는 평가 시즌·성공률이 달라 직접 같지 않다는 것을 이해했다.
- [ ] 같은 OOF로 보정기를 학습하고 다시 평가한 낙관적 점수를 최종 근거로 사용하지 않는다.
- [ ] 팀이 합의한 기준 결과와 동일한 행·fold·평가식으로 새 후보를 비교했다.
- [ ] 새 모델이 악화되면 새 버전이라는 이유로 제출하지 않는다.

## 15. 제출 횟수와 오류

- [ ] 당일 남은 제출 횟수를 확인했다.
- [ ] 일일 최대 제출 수 5회를 넘지 않는다.
- [ ] 설치 오류는 일일 제출 횟수에 반영되지 않는다는 안내를 확인했다.
- [ ] `script.py` 실행 이후 발생하는 제출 오류는 일일 제출 횟수에 반영됨을 확인했다.
- [ ] runtime 확인 없이 시험 삼아 제출하지 않는다.
- [ ] 제출할 ZIP의 모델 설명, hash, 로컬 검증값을 기록했다.
- [ ] 제출 후 리더보드 결과와 제출 ZIP hash를 연결해 기록한다.

## 16. 팀 구성·병합

- [ ] 팀 최대 인원 5명을 넘지 않는다.
- [ ] 팀 구성 전 각 팀원이 최소 1회 제출 기록을 가지고 있다.
- [ ] 팀원 제출 수 합이 대회 경과일 × 일일 최대 5회 이하일 때만 병합 가능한지 당일 팀 페이지에서 확인했다.
- [ ] 팀 병합 마감일을 최신 대회 공지에서 다시 확인했다.
- [ ] 리더보드 제출 마감과 대회 종료 시각을 최신 공지에서 재확인했다.
- [ ] 상위권 코드·PPT 제출 및 코드 검증 대상 여부를 확인했다.

팀·제출 정책은 운영 공지가 바뀔 수 있으므로 로컬 문서보다 대회 페이지의 최신 공지를 최종 기준으로 사용합니다.

## 17. 최종 승인표

| 구분 | 확인 결과 | 담당/일시 |
|---|---|---|
| 금지 정보 미사용 |  |  |
| 외부 데이터·가중치·API 미사용 |  |  |
| test 행 독립성 |  |  |
| Trackman 규칙 |  |  |
| 시간 검증 |  |  |
| 학습·추론 피처 동등성 |  |  |
| submission 형식 |  |  |
| ZIP 구조 |  |  |
| clean install |  |  |
| 245,789행 실행 |  |  |
| 시간·RAM·VRAM |  |  |
| 최종 SHA-256 |  |  |
| 당일 제출 횟수 |  |  |
| 팀 정책 |  |  |

## 18. 최종 제출 판정

아래 네 문장을 모두 사실로 확인한 경우에만 제출합니다.

- [ ] 규칙 위반과 데이터 누수가 없습니다.
- [ ] 새 환경에서 실제 평가 규모 실행을 통과했습니다.
- [ ] ZIP·출력 형식·자원 제한을 모두 통과했습니다.
- [ ] 업로드하려는 파일이 검증한 바로 그 `submit.zip`입니다.

```text
최종 판정: 제출 가능 / 제출 중단
판정 이유:
확인자:
확인 시각:
```

## 참고 자료

- 공식 `data_description.md`
- 대회 개요·평가·규칙·코드 제출 안내 PDF
- Baseline Train·Inference PDF와 공식 baseline 제출 구조
- 사용자가 전달한 외부 모델·API·공식 데이터 제한 및 팀 병합 정책

대회 페이지에 새 공지가 올라오면 이 체크리스트보다 최신 운영 공지가 우선합니다.
